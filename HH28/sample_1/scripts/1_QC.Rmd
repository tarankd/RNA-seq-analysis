---
title: "setup seurat object"
output: html_document
date: "2025-11-25"
fig_width: 6
fig_height: 4
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

#load all libaries required for this script

```{r} 
library(dplyr)
library(Seurat)
library(patchwork)
library(ggplot2)
library(DoubletFinder)
library(tidyverse)
library(RColorBrewer)
library(cowplot)
library(openxlsx)

# colour palette 
my_cols <- brewer.pal(n=5, "Spectral")


## feature and cell count function

FeatCellCount <- function(seuratObj){dim(seuratObj)
              }
  
  
##create function for doublet finder 


preDoublets <- function(seuratObj){  
    seuratObj <- NormalizeData(seuratObj)
    
    seuratObj <- ScaleData(seuratObj)
    seuratObj <- FindVariableFeatures(seuratObj, 
                                      x.low.cutoff = 0.0125, 
                                      y.cutoff = 0.25, 
                                      do.plot=FALSE)
    seuratObj <- RunPCA(seuratObj, pc.genes = seuratObj@var.genes, pcs.print = 0)
    
 
    return(seuratObj)
}



findDoublets <- function(seuratObj, n_pcs=30){
    
   
    nExp_poi  <- round(0.15*nrow(seuratObj@meta.data))
    
    print('yay!')
  
    seuratObj <- doubletFinder(seuratObj, 
                                  PCs = 1:30, 
                                  pN = 0.25, 
                                  pK = 0.01, 
                                  nExp = nExp_poi, 
                                  sct=FALSE)
    
    
    
     seuratObj@meta.data <- seuratObj@meta.data %>% 
        rename_at(vars(starts_with("DF.classifications")), 
                  funs(str_replace(., ".*", "DF.classifications"))) %>%
        rename_at(vars(starts_with("pANN")), 
                  funs(str_replace(., ".*", "pANN")))
    
       
  
        return(seuratObj) 
    
}


```

#load scRNA-seq data set 

```{r}

HH28A.data <- Read10X(data.dir = "/nobackup/proj/comet_icnlmdhuc/Taran/chicken_RNA_seq/results/Sample_1_Results/outs/filtered_feature_bc_matrix/")
```

#assign to seurat object

```{r}

HH28A <- CreateSeuratObject(counts = HH28A.data, project = "HH28", min.cells = 3, min.features = 200)
HH28A


```


#add column to metadata to assign the percentage of mitochondrial dna to

```{r}
HH28A[["percent.mt"]] <- PercentageFeatureSet(HH28A, pattern = "^MT-")
```

#visualise QC metrics as violin plot 

```{r} 
#only for paper, rest is better for accurate sub-setting
QC_metric<- VlnPlot(HH28A, features = c("nFeature_RNA", "nCount_RNA", "percent.mt"), ncol = 3)
QC_metric

```


#feature
```{r}
QC_mf <- VlnPlot(HH28A, features = "nFeature_RNA")
QC_mf
```

```{r}
QC_m.f <- VlnPlot(HH28A, features = "nFeature_RNA", y.max = 1000)
QC_m.f
```

#counts
```{r}
QC_mc <- VlnPlot(HH28A, features = "nCount_RNA")
QC_mc
```

```{r}
QC_m.c <- VlnPlot(HH28A, features = "nCount_RNA", y.max = 2700)
QC_m.c
```

#mitochondrial RNA
```{r}
QC_mm<- VlnPlot(HH28A, features = "percent.mt")
QC_mm
```

```{r}
QC_m.mito<- VlnPlot(HH28A, features = "percent.mt", y.max = 9)
QC_m.mito
```



#feature scatter used to visualise the feature-feature relationships

```{r}

mt<- FeatureScatter(HH28A, feature1 = "nCount_RNA", feature2 = "percent.mt")
feat <- FeatureScatter(HH28A, feature1 = "nCount_RNA", feature2 = "nFeature_RNA")
mtfeat <- FeatureScatter(HH28A, feature1 = "nFeature_RNA", feature2 = "percent.mt")

mt+ feat

mtfeat
```



#subset data to look at specific parameters bc values that are out of scope aren't important and therefore not needed in the graph
#remove mitochondrial genes


```{r}
HH28A <- subset(HH28A, subset = nFeature_RNA > 550 & 
                                percent.mt   <5     &
                                nCount_RNA   >1000  
                )

mt2 <- FeatureScatter(HH28A, feature1 = "nCount_RNA", feature2 = "percent.mt")
feat2 <- FeatureScatter(HH28A, feature1 = "nCount_RNA", feature2 = "nFeature_RNA")

mt2 + feat2

feat2 <- VlnPlot(HH28A, features = "nFeature_RNA", y.max = 4000)

feat2

```

#compare mitchondrial RNA subset data

```{r}
mt + mt2
```

#compare features subset data

```{r}
feat + feat2

```


#nuclei value after subset
```{r}
HH28A
```


#doubletFinder

```{r}

HH28A <- preDoublets(HH28A)
HH28A <- findDoublets(HH28A)

```



```{r}
doublet <- FeatureScatter(HH28A, feature1 = "nCount_RNA", feature2 = "nFeature_RNA", group.by = "DF.classifications")

doublet
```


```{r}
HH28A <- subset(HH28A, subset = DF.classifications != "Doublet" )
```


```{r}
doubsub<- FeatureScatter(HH28A, feature1 = "nCount_RNA", feature2 = "nFeature_RNA", group.by = "DF.classifications")
doubsub
```


#plot after doublet finder comparison

```{r}
mt3<- FeatureScatter(HH28A, feature1 = "nCount_RNA", feature2 = "percent.mt")
feat3 <- FeatureScatter(HH28A, feature1 = "nCount_RNA", feature2 = "nFeature_RNA")

```

#compare mito RNA before and after subset and after doublet finder

```{r}

mt + mt2 + mt3

```

```{r}

feat + feat2 + feat3
```


#nuclei after removal

```{r}
HH28A
```


#checkpoint after QC 

```{r}
saveRDS(HH28A, file = "sObj_QC_1.RDS")
```
